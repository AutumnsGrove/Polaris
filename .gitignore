config.yaml
*.db
*.db-shm
*.db-wal
polaris
logs/
web/node_modules/
web/.svelte-kit/
dev/searxng/data/

# web/build/ is a build artifact (Vite output isn't byte-reproducible
# across runs, so committing it just churns the tree on every rebuild).
# Both local dev and the potato's self-update flow run `pnpm run build`
# before `go build` — see updater/updater.go.
web/build/
