config.yaml
*.db
*.db-shm
*.db-wal
polaris
logs/
web/node_modules/
web/.svelte-kit/
dev/searxng/data/

# web/build/ is intentionally committed — it's the static SvelteKit
# output, embedded into the Go binary via web/embed.go. The potato never
# runs Node; rebuild and commit this locally whenever the frontend changes.
